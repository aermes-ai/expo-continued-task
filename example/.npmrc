# Install the package as a copy (like an app from npm), not a symlink: its config plugin then
# resolves expo from this app's node_modules. Re-run npm install after editing the package.
install-links=true
